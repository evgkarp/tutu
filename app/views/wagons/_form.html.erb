<%= form_with(model: @wagon, local: true) do |f| %>
<%= render 'common/errors', resource: @wagon %>
  <div class="field">
    <%= f.label :variety %>
    <%= f.text_field :variety, id: :wagon_variety %>
  </div>

  <div class="actions">
    <%= f.submit "#{ @wagon.new_record? ? 'Создать' : 'Изменить' }" %>
  </div>
<% end %>